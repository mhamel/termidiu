export function EmptyState() {
  return (
    <section className="empty-state">
      <div className="empty-card">
        <h3>Open a folder to get started</h3>
        <p>
          TERMIDIU watches a project root and detects runnable scripts.
          Click the play button next to a script to launch it in a terminal.
        </p>
      </div>
    </section>
  )
}
